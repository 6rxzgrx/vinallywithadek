import type { Playlist } from '../../interfaces/types';

import { TopShoot } from './topShoot';
import { TopScene } from './topScene';
import { Countdown } from './countdown';

export const biggerHitsPlaylists: Playlist[] = [
	TopShoot,
	TopScene,
	Countdown,
];

